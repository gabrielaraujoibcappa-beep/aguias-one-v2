# QA Gate — v3-B2 UI fila atenção precoce

**Verdito:** PASS (17/09/2026 — Quinn @qa, escopo B2)

## Traceabilidade
- Given amarela com atraso/falta, When abre `/painel/turma`, Then aparece em "Atenção precoce (N)" com motivo + ordenada por nº motivos. Given filtro ativo, Then só precoces; vazio tratado. Sem WhatsApp auto.
- Cobertura: `calcularAtraso7d` (proporção +7d) em `atencao-precoce.test.ts` (4 testes verdes); typecheck limpo. Semáforo/resgate intactos (abaixo: sem alteração de regra).

## Riscos
- Sinal `faltouCall=!checkinEntregue` é proxy grosseiro (sem presença real da call) — calibrar na B3 com dados de presença.
- Fallback nome quando sem `matriculaId` (legado) — aceito, mitigado pelo fix QA-1.

## NFRs
- Sem I/O novo, sem deps, sem gate de papel novo (herda auth da página equipe). Acessibilidade segue padrão (botões + `ResumoFiltrosAtivos`).

## Nota de suite
Full suite 271/273 — 2 falhas em `porta-entrada.test.ts` são da sessão paralela (texto da home mudou, teste desatualizado), fora do escopo B2. Merge da B2 não depende delas, mas o branch só fica verde após aquele fix.
