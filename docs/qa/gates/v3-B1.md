# QA Gate — v3-B1 Regra atenção precoce

**Verdito:** PASS (17/09/2026 — Quinn @qa)

## Traceabilidade
- Given amarela + atraso>0.4 ou falta call → alerta com motivos. Given verde/vermelha ou amarela sem gatilho → sem alerta. Given limiar custom → fila muda. Regressão `verificarNecessidadeResgate` intacta.
- Cobertura: `tests/atencao-precoce.test.ts` (3 testes) + `semaforo-resgate.test.ts` verde; suite 52/265 + typecheck limpos.

## Riscos
- Função pura, sem DB/API/UI — risco mínimo. Sem WhatsApp auto (escopo B2/B3).
- `LIMIAR_ATRASO_ATENCAO_PRECOCE=0.4` é chute inicial — validar com baseline 2 semanas (métrica PRD §3-3).

## NFRs
- Performance trivial (O(1)); segurança n/a (sem I/O); sem novas deps.

## Follow-up
- B2 consumirá a função na UI turma; calibrar limiar com dados reais.
